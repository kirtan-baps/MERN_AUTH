/* eslint-disable no-unused-vars */

import { Link } from "react-router-dom"

const PageNotFound = () => {
    return (
        <div>PageNotFound <Link to={'/login'} >Login</Link></div>
    )
}

export default PageNotFound